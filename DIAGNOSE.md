# Диагностика подключения WebSocket сервера и Pixel Streaming

## Быстрая диагностика

Запустите автоматическую диагностику:

```bash
npm run diagnose
```

Скрипт проверит:
- ✅ Запущен ли WebSocket сервер (порт 3001)
- ✅ Какие порты открыты на сервере
- ✅ На каком порту работает Pixel Streaming
- ✅ Переменные окружения
- ✅ Логи WebSocket сервера

## Ручная проверка

### 1. Проверка WebSocket сервера

```bash
curl http://localhost:3001/health
```

Должно вернуть: `{"status":"healthy"}`

### 2. Проверка открытых портов

```bash
netstat -tlnp | grep LISTEN | grep -E ":(80|8888|8889|3001|5173)"
```

Или через `ss`:
```bash
ss -tlnp | grep LISTEN | grep -E ":(80|8888|8889|3001|5173)"
```

### 3. Проверка Pixel Streaming на разных портах

```bash
# Порт 80
curl -I http://176.97.78.210/?StreamerId=DefaultStreamer

# Порт 8888
curl -I http://176.97.78.210:8888/?StreamerId=DefaultStreamer

# Порт 8889
curl -I http://176.97.78.210:8889/?StreamerId=DefaultStreamer
```

### 4. Просмотр логов WebSocket сервера

```bash
tail -f server.log
```

Ищите строки с `[Proxy]` - там будет видно, к какому адресу пытается подключиться сервер.

### 5. Проверка процесса Pixel Streaming

```bash
ps aux | grep -i pixel
ps aux | grep -i unreal
ps aux | grep -i stream
```

## Решение проблем

### Проблема: Pixel Streaming не найден

**Причина:** Pixel Streaming работает на другом порту или не запущен.

**Решение:**
1. Проверьте логи Pixel Streaming (где он запускается)
2. Найдите порт в логах запуска
3. Настройте переменные окружения:

```bash
nano .env
```

Добавьте:
```env
STREAM_SERVER_HOST=176.97.78.210
STREAM_SERVER_PORT=8888
STREAM_SERVER_PROTOCOL=http
```

### Проблема: WebSocket сервер не подключается к Pixel Streaming

**Причина:** Неправильный хост или порт Pixel Streaming.

**Решение:**
1. Запустите `npm run diagnose`
2. Найдите, на каком порту работает Pixel Streaming
3. Установите переменные окружения (см. выше)
4. Перезапустите WebSocket сервер:

```bash
npm run server:stop
npm run server:bg
```

### Проблема: После смены IP ничего не работает

**Причина:** Pixel Streaming автоматически определяет IP из запроса, но новый IP может быть неправильным.

**Решение:**
1. Определите правильный IP/порт Pixel Streaming через `npm run diagnose`
2. Установите переменные окружения явно
3. Перезапустите все сервисы

## Проверка связи после настройки

1. Проверьте переменные окружения:
```bash
cat .env
```

2. Проверьте логи при запуске WebSocket сервера:
```bash
tail -f server.log
```

Должно быть:
```
🔧 STREAM_SERVER_HOST: 176.97.78.210
🔧 STREAM_SERVER_PORT: 8888
```

3. Откройте в браузере:
```
http://176.97.78.210:5173
```

4. В консоли браузера (F12) проверьте:
- Подключение к WebSocket серверу
- Запросы к `/api/proxy/streamers`
- Ошибки подключения

