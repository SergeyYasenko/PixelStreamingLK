# 🚀 Быстрый старт с ngrok

Инструкция по подключению через ngrok туннель для тестирования приложения.

## 📋 Предварительные требования

1. **Установленный ngrok**
   - Если не установлен: https://ngrok.com/download
   - Или через npm: `npm install -g ngrok`

2. **Авторизация ngrok** (если еще не сделано)
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```
   Получить authtoken: https://dashboard.ngrok.com/get-started/your-authtoken

## 🎯 Пошаговая инструкция

### Шаг 1: Запустите фронтенд

**Терминал 1:**
```bash
npm run dev
```

Фронтенд запустится на `http://localhost:5173`

### Шаг 2: Запустите ngrok туннель

**Терминал 2:**
```bash
npm run tunnel:ngrok
```

Или напрямую:
```bash
ngrok http 5173
```

### Шаг 3: Получите публичный URL

После запуска ngrok вы увидите что-то вроде:
```
Forwarding  https://xxxx-xx-xx-xx-xx.ngrok-free.app -> http://localhost:5173
```

Скопируйте URL: `https://xxxx-xx-xx-xx-xx.ngrok-free.app`

### Шаг 4: Откройте приложение

Откройте полученный ngrok URL в браузере:
```
https://xxxx-xx-xx-xx-xx.ngrok-free.app
```

### Шаг 5: Настройте переменные окружения (если нужно)

Если используете бэкенд на Railway, убедитесь что в `.env.local` указан правильный URL бэкенда:
```env
VITE_WS_SERVER_URL=https://your-backend.up.railway.app
```

**⚠️ ВАЖНО**: Используйте URL бэкенда на Railway, НЕ URL ngrok!

## 🔧 Альтернативные команды

### Для бэкенда (WebSocket сервер на порту 3001):
```bash
ngrok http 3001
```

### С постоянным доменом (требует бесплатный аккаунт ngrok):
```bash
ngrok http 5173 --domain=your-domain.ngrok-free.app
```

## ✅ Проверка

1. ✅ Фронтенд запущен на `http://localhost:5173`
2. ✅ ngrok туннель активен и показывает публичный URL
3. ✅ Открываете ngrok URL в браузере
4. ✅ Приложение загружается и работает
5. ✅ WebSocket подключается к бэкенду (проверьте консоль браузера)

## 🐛 Решение проблем

### Ошибка "ngrok: command not found"
- Установите ngrok: https://ngrok.com/download
- Или через npm: `npm install -g ngrok`

### Ошибка "authtoken required"
- Зарегистрируйтесь на https://ngrok.com
- Получите authtoken: https://dashboard.ngrok.com/get-started/your-authtoken
- Выполните: `ngrok config add-authtoken YOUR_TOKEN`

### Страница ngrok показывает предупреждение
- Нажмите "Visit Site" для продолжения
- Это нормально для бесплатного плана ngrok

### Приложение не загружается через ngrok
- Убедитесь, что фронтенд запущен на порту 5173
- Проверьте, что ngrok указывает на правильный порт: `ngrok http 5173`
- Перезапустите dev-сервер после запуска ngrok

## 📝 Примечания

- **ngrok URL меняется** при каждом запуске (если не используете постоянный домен)
- Для **постоянного URL** лучше задеплоить на Vercel/Netlify
- **Бесплатный план ngrok** имеет ограничения по трафику и времени работы
- **WebSocket** работает через ngrok автоматически

## 🎉 Готово!

Теперь вы можете:
- Поделиться ngrok URL с другими пользователями
- Тестировать приложение из любого места
- Проверять работу на разных устройствах

