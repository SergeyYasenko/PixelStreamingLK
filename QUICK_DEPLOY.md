# 🚀 Быстрый деплой - Все по одной ссылке

## Самый простой способ: Railway (Бэкенд) + Локальный фронтенд с туннелем

### Шаг 1: Деплой бэкенда на Railway

1. Зайдите на https://railway.app
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. В настройках сервиса:
   - **Start Command**: `node server-example.js`
   - Railway автоматически определит порт
5. После деплоя получите URL: `https://your-backend.up.railway.app`
6. Проверьте: откройте URL в браузере, должно быть `{"status":"ok",...}`

### Шаг 2: Запустите фронтенд локально с туннелем

**Терминал 1 - Фронтенд:**
```bash
npm run dev
```

**Терминал 2 - Туннель:**
```bash
npm run tunnel:frontend
# или
lt --port 5173
```

Вы получите URL вида: `https://random-name.loca.lt`

### Шаг 3: Настройте переменные окружения

Создайте файл `.env.local`:
```env
VITE_WS_SERVER_URL=https://your-backend.up.railway.app
```

### Шаг 4: Перезапустите фронтенд

Остановите и снова запустите:
```bash
npm run dev
```

### Шаг 5: Готово! 🎉

Теперь все могут открыть `https://random-name.loca.lt` и подключаться!

**Для постоянного URL используйте Vercel (см. ниже)**

---

## Альтернатива: Vercel (Фронтенд) + Railway (Бэкенд) - Для постоянного URL

### Фронтенд на Vercel:

1. Зайдите на https://vercel.com
2. "New Project" → Импортируйте GitHub репозиторий
3. Настройки:
   - Framework Preset: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Добавьте переменную окружения:
   ```
   VITE_WS_SERVER_URL=https://your-backend.railway.app
   ```
5. Деплой!

### Бэкенд на Railway:

1. Зайдите на https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Выберите `server-example.js`
5. Railway автоматически запустит сервер

---

## Что получится:

✅ Одна ссылка для всех: `https://your-app.vercel.app`  
✅ Все подключаются к одному серверу  
✅ Не нужно ничего скачивать и запускать локально  
✅ Бесплатно для небольших проектов  

---

## Важно:

1. **WebSocket поддержка**: Railway и Render поддерживают WebSocket
2. **Переменные окружения**: Используйте их для URL сервера
3. **CORS**: Сервер уже настроен на `origin: "*"`, так что будет работать
4. **HTTPS**: Все хостинги предоставляют HTTPS автоматически

---

## Если что-то не работает:

1. Проверьте логи в панели хостинга
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что бэкенд запущен и доступен
4. Проверьте консоль браузера на ошибки

