# Настройка IP после смены адреса сервера

## Структура сервисов

- **Порт 5173** - Фронтенд (Vite dev server)
- **Порт 3001** - WebSocket сервер (для синхронизации пользователей)
- **Порт 80** - Pixel Streaming сервер (видео-трансляция)

## Настройка после смены IP

### Шаг 1: Создайте файл `.env` на сервере

```bash
nano .env
```

### Шаг 2: Добавьте следующие строки

```env
# IP адрес сервера
STREAM_SERVER_HOST=176.97.78.210

# Порт Pixel Streaming (всегда 80)
STREAM_SERVER_PORT=80

# Протокол (http или https)
STREAM_SERVER_PROTOCOL=http
```

**Важно:** Замените `176.97.78.210` на ваш текущий IP адрес!

### Шаг 3: Перезапустите WebSocket сервер

```bash
npm run server:stop
npm run server:bg
```

### Шаг 4: Проверьте логи

```bash
tail -f server.log
```

Вы должны увидеть:
```
🔧 STREAM_SERVER_HOST: 176.97.78.210
🔧 STREAM_SERVER_PORT: 80
```

## Проверка работы

1. **Откройте в браузере:**
   ```
   http://176.97.78.210:5173
   ```

2. **Выберите комнату** - должна появиться форма входа

3. **После входа** - должен открыться iframe с Pixel Streaming с порта 80

## Автоматический запуск при старте системы

Если Pixel Streaming запускается автоматически, убедитесь, что:

1. Pixel Streaming настроен на порт 80
2. Переменные окружения указаны в `.env`
3. WebSocket сервер запускается после Pixel Streaming

## Устранение проблем

### Проблема: Сервер не находит streamers

**Решение:**
1. Проверьте, что Pixel Streaming запущен на порту 80:
   ```bash
   curl http://176.97.78.210/?StreamerId=DefaultStreamer
   ```

2. Проверьте переменные окружения:
   ```bash
   cat .env
   ```

3. Перезапустите WebSocket сервер:
   ```bash
   npm run server:stop
   npm run server:bg
   ```

### Проблема: Iframe не загружается

**Решение:**
- Фронтенд автоматически определяет хост из `window.location.hostname`
- Убедитесь, что открываете приложение по правильному IP: `http://176.97.78.210:5173`
- Проверьте консоль браузера (F12) на наличие ошибок CORS

## Быстрая команда для настройки

```bash
# Создать .env файл
cat > .env << EOF
STREAM_SERVER_HOST=176.97.78.210
STREAM_SERVER_PORT=80
STREAM_SERVER_PROTOCOL=http
EOF

# Перезапустить сервер
npm run server:stop && npm run server:bg

# Проверить логи
tail -20 server.log
```

**Не забудьте заменить IP на ваш текущий!**

