# 🔧 Решение проблем с подключением

## Проблема: Коллега не может подключиться (бесконечная загрузка)

### Возможные причины:

1. **WebSocket не может подключиться к бэкенду**
2. **Бэкенд на Railway недоступен**
3. **Неправильный URL в переменных окружения**
4. **Localtunnel не поддерживает WebSocket должным образом**

---

## Решение 1: Проверка бэкенда на Railway

### Шаг 1: Проверьте доступность бэкенда

1. Откройте URL бэкенда в браузере (например: `https://your-backend.up.railway.app`)
2. Должно быть: `{"status":"ok","message":"WebSocket server is running","connections":0}`
3. Если не открывается - бэкенд не работает или URL неправильный

### Шаг 2: Проверьте логи на Railway

1. Зайдите на https://railway.app
2. Откройте ваш проект → сервис с бэкендом
3. Перейдите в "Deployments" → "View Logs"
4. Проверьте, есть ли ошибки

### Шаг 3: Проверьте переменные окружения

Убедитесь, что в `.env.local` указан правильный URL:
```env
VITE_WS_SERVER_URL=https://your-backend.up.railway.app
```

**НЕ используйте:**
- ❌ `http://localhost:3001`
- ❌ URL туннеля фронтенда
- ❌ `https://famous-ghosts-own.loca.lt`

---

## Решение 2: Деплой фронтенда на Vercel (Рекомендуется!)

Localtunnel может не работать стабильно для WebSocket. Лучше задеплоить фронтенд на Vercel:

### Шаг 1: Деплой на Vercel

1. Зайдите на https://vercel.com
2. "New Project" → Импортируйте GitHub репозиторий
3. Настройки:
   - Framework Preset: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Добавьте переменную окружения:
   ```
   VITE_WS_SERVER_URL=https://your-backend.up.railway.app
   ```
5. Деплой!

### Шаг 2: Поделитесь ссылкой

После деплоя получите постоянный URL: `https://your-project.vercel.app`

Теперь все могут открыть эту ссылку и подключиться!

---

## Решение 3: Отладка подключения

### Проверьте консоль браузера (F12)

Должны быть сообщения:
- ✅ `🔌 Connecting to WebSocket server: https://your-backend.up.railway.app`
- ✅ `✅ WebSocket connected: [socket-id]`
- ✅ `📝 Joining room: [room-id]`

Если видите ошибки:
- ❌ `❌ WebSocket connection error: timeout` - бэкенд недоступен
- ❌ `❌ WebSocket connection error: CORS` - проблема с CORS
- ❌ `❌ WebSocket connection error: 404` - неправильный URL

### Проверьте Network tab

1. Откройте DevTools (F12)
2. Перейдите в "Network"
3. Найдите запросы к `/socket.io/`
4. Проверьте статус ответа:
   - 200/101 - OK
   - 404 - неправильный URL
   - 500 - ошибка сервера
   - timeout - сервер недоступен

---

## Решение 4: Использование ngrok вместо localtunnel

Localtunnel может не работать стабильно. Попробуйте ngrok:

### Установка ngrok:

1. Зарегистрируйтесь на https://ngrok.com (бесплатно)
2. Скачайте ngrok
3. Получите authtoken

### Использование:

```bash
ngrok http 5173
```

Вы получите URL вида: `https://xxxx-xx-xx-xx-xx.ngrok-free.app`

### Обновите vite.config.js:

```javascript
allowedHosts: [
  'localhost',
  '.loca.lt',
  '.ngrok.io',
  '.ngrok-free.app',
],
```

---

## Быстрая проверка:

1. ✅ Бэкенд на Railway работает? (откройте URL в браузере)
2. ✅ Правильный URL в `.env.local`?
3. ✅ Dev-сервер перезапущен после изменения `.env.local`?
4. ✅ В консоли браузера есть ошибки?
5. ✅ WebSocket подключается? (проверьте Network tab)

---

## Рекомендация:

**Самый надежный способ**: 
- Бэкенд на Railway ✅
- Фронтенд на Vercel ✅

Это даст постоянный URL и стабильную работу WebSocket!


